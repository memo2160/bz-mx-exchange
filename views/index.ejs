<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exchange Rate Alerts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            max-width: 900px;
        }
        .image-container {
            background: url('/images/mexico-image.jpg') center/cover no-repeat;
            height: 100%;
            min-height: 300px;
            border-radius: 10px;
        }
        .card {
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row g-4">
            <!-- Image Section -->
            <div class="col-md-6 d-none d-md-block">
                <div class="image-container w-100"></div>
            </div>

            <!-- Subscription Form -->
            <div class="col-md-6">
                <div class="card text-center">
                    <h2 class="mb-3">BZ/MX Exchange Alerts</h2>
                    <form action="/subscribe" method="POST" class="mb-3">
                        <input type="email" class="form-control mb-2" name="email" placeholder="Enter email to subscribe" required>
                        <button type="submit" class="btn btn-primary">Subscribe</button>
                    </form>
                    <form action="/unsubscribe" method="POST" class="mb-3">
                        <input type="email" class="form-control mb-2" name="email" placeholder="Enter email to unsubscribe" required>
                        <button type="submit" class="btn btn-outline-danger">Unsubscribe</button>
                    </form>
                    <div class="alert text-center" style="color: <%= exchangeRateMessage.color %>; font-weight: bold;">
                        <%= exchangeRateMessage.text %>
                    </div>
                    <a href="/disclaimer" class="btn btn-link">Read Disclaimer</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
