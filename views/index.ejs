<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sales Alert Subscription</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light background for better contrast */
        }
        h2 {
            font-size: 1.5rem; /* Adjust heading size */
        }
        .btn {
            padding: 10px 20px; /* Increase button size */
            font-size: 1rem; /* Adjust button text size */
        }
        .alert {
            font-size: 0.9rem; /* Adjust alert text size */
        }
        .container {
            max-width: 500px;
            margin: 0 auto;
        }
        .alert-message {
            display: none; /* Hide the message by default */
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Subscribe for BZ/MX Exchange Alerts</h2>
        <form action="/subscribe" method="POST" id="subscribeForm" class="mt-3">
            <div class="mb-3">
                <label for="email" class="form-label">Email address</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Subscribe</button>
        </form>

        <!-- Success or Error Message -->
        <div id="alertMessage" class="alert alert-dismissible mt-3 alert-message" role="alert">
            <span id="alertText"></span>
        </div>

        <div id="exchangeRateMessage" class="alert mt-4 text-center" style="color: <%= exchangeRateMessage.color %>; font-weight: bold;">
            <%= exchangeRateMessage.text %>
        </div>
        <a href="/disclaimer" class="btn btn-link">Read Disclaimer</a>
    </div>
</body>
</html>
